version: '3.9'
services:
    app:
        build:
            context: ./
            dockerfile: Dockerfile
            target: development
        image: urlencoder_dev
        container_name: urlencoder_dev
        volumes:
            - ./:/app
        tty: true
