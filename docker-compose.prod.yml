version: '3.9'
services:
    app:
        build:
            context: ./
            dockerfile: Dockerfile
            target: production
        image: urlencoder_prod
        container_name: urlencoder_prod
        tty: true
