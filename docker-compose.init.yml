version: '3.9'
services:
    app:
        build:
            context: ./
            dockerfile: Dockerfile
            target: initial
        volumes:
            - ./:/opt/pysetup
        tty: true
